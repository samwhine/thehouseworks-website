# Base URL used for canonical links, sitemap, robots, and JSON-LD.
# Currently set to the Vercel preview/default domain. Swap this to the
# real custom domain once one is attached (spec section 60).
NEXT_PUBLIC_SITE_URL=https://thehouseworks.vercel.app
